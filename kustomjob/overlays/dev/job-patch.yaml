spec:
  template:
    spec:
      containers:
        - name: example-container
          image: "nginx:1.21"  # Dev version
          resources:
            limits:
              cpu: "100m"
              memory: "128Mi"
            requests:
              cpu: "50m"
              memory: "64Mi"

