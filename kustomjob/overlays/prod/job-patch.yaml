spec:
  template:
    spec:
      containers:
        - name: example-container
          image: "nginx:stable"  # Production version
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"

