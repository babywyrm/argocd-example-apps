spec:
  template:
    spec:
      containers:
        - name: example-container
          image: "nginx:1.22"  # Staging version
          resources:
            limits:
              cpu: "200m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"

